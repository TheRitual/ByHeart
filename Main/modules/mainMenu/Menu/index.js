import React from 'react';
import { Text, View } from 'react-native';

const Menu = (props) => {
    return (
        <View {...props}>
            <Text>MENU</Text>
        </View>
    );
}

export default Menu;